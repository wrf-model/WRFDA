# JRB
# No need for clever logic once wrf_io_flags.h and status_codes.h only exist in 
# one version here

superclean : clean

clean :
	@ $(RM) *.o *.f *.mod

# DEPENDENCIES : only dependencies after this line 

