
clean :
	@ ( cd esmf_time_f90 ; make clean )
	@ ( cd io_netcdf ;  make clean )
	@ ( cd io_int ; make clean )
	@ ( cd lapack ; make clean )
	@ ( cd blas ; make clean )
	@ ( cd bufr_ncep_nco ; make clean )
	@ ( cd fftpack5 ; make clean )
	@ ( cd io_mcel ;  make clean )
	@ ( cd io_phdf5 ; make clean )
	@ ( cd io_grib1 ; make clean )
	@ ( cd io_esmf ;  make clean )
	@ ( cd ioapi_share ;  make clean )
	@ ( cd RSL/RSL ; make clean )
	@ ( cd RSL_LITE ; make clean )

superclean : clean
	@ ( cd esmf_time_f90 ;  make superclean )
	@ ( cd io_netcdf ;  make superclean )
	@ ( cd io_int ; make superclean )
	@ ( cd lapack ; make superclean )
	@ ( cd blas ; make superclean )
	@ ( cd bufr_ncep_nco ; make superclean )
	@ ( cd fftpack5 ; make superclean )
	@ ( cd io_mcel ;  make superclean )
	@ ( cd io_phdf5 ; make superclean )
	@ ( cd io_grib1 ; make clean )
	@ ( cd io_esmf ;  make superclean )
	@ ( cd ioapi_share ;  make superclean )
	@ ( cd RSL/RSL ; make superclean )
	@ ( cd RSL_LITE ; make superclean )


