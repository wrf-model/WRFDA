#!/bin/ksh
export CPP=/opt/ctl/bin/cpp
export CPPFLAGS="-C -P"
export FFCFLAGS="-O0 -f fixed -sreal64 -dp"
export FFLAGS="-O0 -f fixed -sreal64 -dp"
export FC=ftn
export FFC=ftn
export LDFLAGS=
export TARGET=
export LD="ar -ru libfftpack5.a"
rm *.a
make clean
make library

